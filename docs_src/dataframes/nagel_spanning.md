## Description

One tidy row with the spanning-regression results: the VoC strategy's monthly returns regressed on the momentum-benchmark strategy and the static standardized market (`alpha`, `alpha_tstat`, `beta_benchmark`, `beta_market`, `resid_std`), plus the anatomy regression's `anatomy_r2` and `anatomy_intercept`. An insignificant alpha here is the quantitative core of the critique: the complex strategy adds little beyond the simple benchmark. Built by `doit nagel`; keyed by the sample period.
