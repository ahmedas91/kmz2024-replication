# Dependencies for KMZ 2024 Replication
# Install with: pip install -r requirements.txt
# Every package here is either imported by src/ or voc/, or invoked by a
# doit task (jupyter/jupytext for the notebook build, chartbook for the
# site, ruff for lint/format). LaTeX (latexmk/xelatex) comes from TeXLive,
# not pip.

# Analysis
joblib
matplotlib>=3.9.2
numpy>=1.26.4
pandas>=2.2.3
pyarrow
scikit-learn>=1.5.2

# Data access
openpyxl>=3.1.5
pandas-datareader>=0.10.0
requests>=2.32.3
wrds>=3.2.0

# Configuration and build
doit>=0.36.0
python-decouple>=3.8

# Tests and lint
pytest>=8.3.3
ruff

# Notebook build and docs site
jupyter
jupytext
notebook
chartbook[all]>=0.0.14
plotly>=5.24.1  # used by the example notebook only; retire with issue #13
